import React from 'react';

const AdminLayout = ({ children }:any) => {
    
  return (
    <div>
      {/* Admin-specific layout components go here */}
      {children}
    </div>
  );
};

export default AdminLayout;
